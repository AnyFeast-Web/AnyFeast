name: anyfeast_app
description: AnyFeast Nutritionist Platform App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management & Routing
  flutter_riverpod: ^2.4.3
  go_router: ^12.1.1
  
  # Networking
  dio: ^5.3.3
  shared_preferences: ^2.2.1
  
  # Firebase removed
  
  # UI & Styling
  google_fonts: ^6.1.0
  flutter_lucide: ^1.8.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true